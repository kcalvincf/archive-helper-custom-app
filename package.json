{
  "name": "contentful-archive-helper",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Contentful app: inbound link insights, safe unlink, App Actions",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @archive-helper/shared-types && npm run build -w @archive-helper/utils && npm run build -w @archive-helper/backend && npm run build -w @archive-helper/sidebar",
    "build:packages": "npm run build -w @archive-helper/shared-types -w @archive-helper/utils",
    "test": "npm run test -w @archive-helper/utils -w @archive-helper/backend --if-present",
    "dev:backend": "npm run dev -w @archive-helper/backend",
    "dev:sidebar": "npm run dev -w @archive-helper/sidebar"
  },
  "engines": {
    "node": ">=18.18"
  }
}
